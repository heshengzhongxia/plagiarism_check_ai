function RightPanel() {
  return (
    <aside className="w-[360px] shrink-0 border-l border-[var(--border)] bg-[var(--bg)]/40 overflow-y-auto">
      <div className="p-4">
        <h2 className="text-sm font-semibold text-[var(--muted)] uppercase tracking-wider mb-3">
          报告
        </h2>
        <p className="text-xs text-[var(--muted)]">报告面板将在 Task 16 中实现。</p>
      </div>
    </aside>
  );
}

export default RightPanel;
