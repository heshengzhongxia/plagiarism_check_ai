function CenterPanel() {
  return (
    <section className="flex-1 min-w-0 overflow-y-auto bg-[var(--bg)]/20">
      <div className="p-4">
        <h2 className="text-sm font-semibold text-[var(--muted)] uppercase tracking-wider mb-3">
          对话流
        </h2>
        <p className="text-xs text-[var(--muted)]">对话流面板将在 Task 15 中实现。</p>
      </div>
    </section>
  );
}

export default CenterPanel;
